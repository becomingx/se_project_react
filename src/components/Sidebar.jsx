import "../blocks/Sidebar.css";

//SideBar displays the current user's username and avatar (still hardcoded).

const Sidebar = () => {
    return <p className="Sidebar"></p>
}

export default Sidebar;